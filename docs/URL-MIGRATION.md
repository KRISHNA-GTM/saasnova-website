# URL migration and redirects

_Generated by `scripts/build-url-doc.mjs`. Edit `src/data/redirects.mjs`, then regenerate._

## New URL scheme

Clean, lowercase, hyphenated paths with no `.html` and no trailing slash, grouped by section:
`/programs/*`, `/services/*`, `/solutions/*`, `/partners/*`, `/community/*`, `/insights/*`, `/news/*`,
`/track-record/*`, `/campaigns/*`, `/events/*`. Canonical host: `https://www.saasnova.ai`. Every page emits a
canonical tag that matches its clean URL, and the sitemap uses the same form.

Indexable pages (46, from the generated sitemap):

- `/`
- `/about`
- `/careers`
- `/community/aws`
- `/community/azure`
- `/community/gcp`
- `/contact`
- `/customers`
- `/events`
- `/fq-source`
- `/insights`
- `/insights/aws-alignment`
- `/insights/aws-prm-deadline`
- `/insights/cloud-marketplace-listing-silent-fix`
- `/insights/stop-buying-software-like-2015`
- `/news`
- `/news/aws-advanced-tier-partner`
- `/news/carahsoft-saasnova-aws-marketplace-gtm`
- `/news/feenix-aws-marketplace-storefront`
- `/newsletter`
- `/partners`
- `/partners/carahsoft`
- `/partners/pronix`
- `/partners/saasify`
- `/partners/workspan`
- `/programs`
- `/programs/ignite`
- `/programs/novax`
- `/programs/supernova`
- `/services`
- `/services/alliance-leadership`
- `/services/aws-marketplace-listing`
- `/services/box-funding-readiness`
- `/services/co-marketing`
- `/services/co-sell-execution`
- `/services/genai-competency-readiness`
- `/services/partner-central-migration`
- `/services/partner-revenue-measurement`
- `/services/storefront`
- `/show`
- `/solutions/channel-partners`
- `/solutions/isvs`
- `/storefront`
- `/support`
- `/track-record/arctic-wolf`
- `/track-record/dataminr`

Also published but kept out of the sitemap: `/privacy` and `/terms` (noindex, matching the legacy robots
policy), `/404`, `/insights/rss.xml`, `/llms.txt`, `/llms-full.txt`, `/robots.txt`, `/assistant-kb.json`.

## How redirects work on S3 + CloudFront

S3 does not read the legacy `_redirects` file (that is a Netlify / Cloudflare Pages format), and the
legacy 404.html JavaScript redirect is invisible to search engines. The new site uses a **CloudFront
Function** on viewer request, generated at build time from `src/data/redirects.mjs` into
`deploy/cloudfront-function.js`:

1. `saasnova.ai/*` to `https://www.saasnova.ai/*` (301)
2. Legacy paths, with or without `.html`, in any letter case, to the new URL (301). Query strings,
   including UTM parameters, are preserved.
3. `/about/` and `/about/index.html` to `/about` (301)
4. Any other `/x.html` to `/x` (301)
5. `/programs/ignite` is served internally from `/programs/ignite/index.html` (no redirect)

`npm run test:edge` runs 21 cases against the generated function, including `/press&media.html`
and `/press%26media.html`.

## Legacy to new (301)

| Legacy URL | New URL |
|---|---|
| `/index` (and `/index.html`) | `/` |
| `/ignite` (and `/ignite.html`) | `/programs/ignite` |
| `/supernova` (and `/supernova.html`) | `/programs/supernova` |
| `/novax` (and `/novax.html`) | `/programs/novax` |
| `/pcmaas` (and `/pcmaas.html`) | `/services/partner-central-migration` |
| `/pdmaas` (and `/pdmaas.html`) | `/services/co-sell-execution` |
| `/pmmaas` (and `/pmmaas.html`) | `/services/co-marketing` |
| `/prmaas` (and `/prmaas.html`) | `/services/partner-revenue-measurement` |
| `/palaas` (and `/palaas.html`) | `/services/alliance-leadership` |
| `/storefront-saasnova` (and `/storefront-saasnova.html`) | `/storefront` |
| `/partner-carahsoft` (and `/partner-carahsoft.html`) | `/partners/carahsoft` |
| `/partner-saasify` (and `/partner-saasify.html`) | `/partners/saasify` |
| `/partner-workspan` (and `/partner-workspan.html`) | `/partners/workspan` |
| `/partner-pronix` (and `/partner-pronix.html`) | `/partners/pronix` |
| `/partner-aws-gtm` (and `/partner-aws-gtm.html`) | `/community/aws` |
| `/partner-azure-gtm` (and `/partner-azure-gtm.html`) | `/community/azure` |
| `/partner-gcp-gtm` (and `/partner-gcp-gtm.html`) | `/community/gcp` |
| `/case-study` (and `/case-study.html`) | `/customers` |
| `/results` (and `/results.html`) | `/customers` |
| `/case-study-dataminr` (and `/case-study-dataminr.html`) | `/track-record/dataminr` |
| `/dataminr` (and `/dataminr.html`) | `/track-record/dataminr` |
| `/case-study-arcticwolf` (and `/case-study-arcticwolf.html`) | `/track-record/arctic-wolf` |
| `/arctic-wolf` (and `/arctic-wolf.html`) | `/track-record/arctic-wolf` |
| `/blog` (and `/blog.html`) | `/insights` |
| `/blog-aws-prm-deadline` (and `/blog-aws-prm-deadline.html`) | `/insights/aws-prm-deadline` |
| `/blog-aws-alignment` (and `/blog-aws-alignment.html`) | `/insights/aws-alignment` |
| `/blog-cloud-marketplace-listing-silent-fix` (and `/blog-cloud-marketplace-listing-silent-fix.html`) | `/insights/cloud-marketplace-listing-silent-fix` |
| `/blog-multi-cloud-gtm` (and `/blog-multi-cloud-gtm.html`) | `/insights` |
| `/saasnova-hydrolix-blog-stop-buying-software-like-2015` (and `/saasnova-hydrolix-blog-stop-buying-software-like-2015.html`) | `/insights/stop-buying-software-like-2015` |
| `/news-press` (and `/news-press.html`) | `/news` |
| `/press&media` (and `/press&media.html`) | `/news` |
| `/press%26media` (and `/press%26media.html`) | `/news` |
| `/blog-carahsoft-saasnova-accelerating-aws-marketplace-gtm-execution-to-isvs-and-channel-partners` (and `/blog-carahsoft-saasnova-accelerating-aws-marketplace-gtm-execution-to-isvs-and-channel-partners.html`) | `/news/carahsoft-saasnova-aws-marketplace-gtm` |
| `/saasnova-achieves-aws-advanced-tier-partner-status` (and `/saasnova-achieves-aws-advanced-tier-partner-status.html`) | `/news/aws-advanced-tier-partner` |
| `/saasnova-feenix-pr-aws-marketplace-storefront` (and `/saasnova-feenix-pr-aws-marketplace-storefront.html`) | `/news/feenix-aws-marketplace-storefront` |
| `/webinars` (and `/webinars.html`) | `/events` |
| `/chatbot` (and `/chatbot.html`) | `/` |
| `/fq-source - Copy` (and `/fq-source - Copy.html`) | `/fq-source` |
| `/404` (and `/404.html`) | `/` |

Legacy pages whose path did not change (`/about`, `/services`, `/partners`, `/careers`, `/contact`,
`/support`, `/privacy`, `/terms`, `/newsletter`, `/show`, `/fq-source`) redirect from their `.html`
form to the clean URL.

## Migration checklist and risks

- **Back up** the current bucket (enable versioning or copy to a backup prefix) before the first deploy.
- **Attach the CloudFront Function before or with the first deploy.** `deploy.ps1` uses `--delete`, which
  removes the legacy `.html` objects. Their URLs keep working only because the function redirects them
  before the request reaches S3.
- **Search Console**: verification is preserved (`/google960def4cf195e9cf.html` is kept, and the
  `google-site-verification` meta tag is on every page). After launch, submit
  `https://www.saasnova.ai/sitemap-index.xml` and inspect the top legacy URLs.
- **Expect** a short ranking fluctuation while Google processes the 301s. Watch the Search Console
  "Pages" report for 404s for 2 to 4 weeks and add any missing legacy paths to `redirects.mjs`.
- **AWS Marketplace listings** link to `/`, `/contact` and `/support`, which all still resolve. Consider
  pointing each listing's resources at its own page (for example `/programs/ignite`).
- **Case studies moved** from `/case-study-*` to `/track-record/*`, because the work was delivered during
  Jen Dawson's AWS tenure (see STRATEGY.md).
- After launch, delete the legacy `_redirects`, `.nojekyll` and the 404.html redirect script from the repo.
