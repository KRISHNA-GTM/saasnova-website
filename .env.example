# Copy to .env for local builds. Values here are baked into the static site at build time,
# so never put secrets in PUBLIC_* variables.
#
# Enables live mode for the "Ask SaaSNova" assistant once the chat API is deployed behind
# CloudFront at /api/chat. Leave empty to keep guided mode.
PUBLIC_CHAT_ENDPOINT=
